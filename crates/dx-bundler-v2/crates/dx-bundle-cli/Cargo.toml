[package]
name = "dx-bundle-cli"
description = "CLI for DX Bundler v2 - 3x faster than Bun"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "dx-bundle"
path = "src/main.rs"

[dependencies]
dx-bundle-core = { path = "../dx-bundle-core" }
dx-bundle-simd = { path = "../dx-bundle-simd" }
dx-bundle-pipeline = { path = "../dx-bundle-pipeline" }
dx-bundle-cache = { path = "../dx-bundle-cache" }
dx-bundle-parallel = { path = "../dx-bundle-parallel" }
dx-bundle-delta = { path = "../dx-bundle-delta" }
dx-bundle-emit = { path = "../dx-bundle-emit" }
clap.workspace = true
anyhow.workspace = true
tokio.workspace = true
