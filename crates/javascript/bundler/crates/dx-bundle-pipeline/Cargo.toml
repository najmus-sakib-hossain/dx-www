[package]
name = "dx-bundle-pipeline"
description = "Unified single-pass transformation pipeline for DX Bundler v2"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
dx-bundle-core = { path = "../dx-bundle-core" }
dx-bundle-scanner = { path = "../dx-bundle-scanner" }
thiserror.workspace = true
