[package]
name = "dx-bundle-concat"
version = "0.1.0"
edition = "2024"
authors = ["DX Team"]
description = "Zero-copy module concatenation for DX bundler"

[dependencies]
dx-bundle-core = { path = "../dx-bundle-core" }
memmap2 = { workspace = true }

[lib]
name = "dx_bundle_concat"
path = "src/lib.rs"