[package]
name = "dx-bundle-transform"
version = "0.1.0"
edition = "2024"
authors = ["DX Team"]
description = "SIMD-accelerated transformations for DX bundler"

[dependencies]
dx-bundle-core = { path = "../dx-bundle-core" }
rayon = { workspace = true }

[lib]
name = "dx_bundle_transform"
path = "src/lib.rs"