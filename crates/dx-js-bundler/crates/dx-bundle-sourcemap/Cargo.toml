[package]
name = "dx-bundle-sourcemap"
version = "0.1.0"
edition = "2024"
authors = ["DX Team"]
description = "Binary source maps for DX bundler"

[dependencies]
dx-bundle-core = { path = "../dx-bundle-core" }
bytemuck = { workspace = true }

[lib]
name = "dx_bundle_sourcemap"
path = "src/lib.rs"