[package]
name = "dx-bundle-core"
version = "0.1.0"
edition = "2024"
authors = ["DX Team"]
description = "Core types and binary formats for DX bundler"

[dependencies]
bytemuck = { workspace = true }
memmap2 = { workspace = true }

[lib]
name = "dx_bundle_core"
path = "src/lib.rs"
