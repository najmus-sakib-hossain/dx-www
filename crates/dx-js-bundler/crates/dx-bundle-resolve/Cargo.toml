[package]
name = "dx-bundle-resolve"
version = "0.1.0"
edition = "2024"
authors = ["DX Team"]
description = "Parallel import resolution for DX bundler"

[dependencies]
dx-bundle-core = { path = "../dx-bundle-core" }
rayon = { workspace = true }
dashmap = { workspace = true }

[lib]
name = "dx_bundle_resolve"
path = "src/lib.rs"